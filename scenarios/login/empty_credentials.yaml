# Scenario: Empty Credentials Submission Validation
# Tests that submitting empty credentials triggers client-side validation errors.
id: empty_credentials
name: Empty credentials login validation attempt
tags: [regression, login, validation]
steps:
  - inspect_widget_tree: true
  - tap:
      key: login.submit
assertions:
  - widget_present: login.id
